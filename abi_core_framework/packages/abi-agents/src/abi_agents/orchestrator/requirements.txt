# Orchestrator Agent Dependencies
# Ensure latest abi-core is installed
abi-core-ai>=1.12.42

# All other base dependencies inherited from abi-image
# Add only orchestrator-specific dependencies here if needed
