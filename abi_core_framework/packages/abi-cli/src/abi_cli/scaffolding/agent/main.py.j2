#!/usr/bin/env python3
"""{{ agent_name }} Agent — Generated by ABI-Core scaffolding"""

from {{ agent_file_name }} import {{ agent_class_name }}
{% if with_web_interface %}from web_interface import AgentWebInterface
{% endif %}from abi_core.agent import AbiCore

agent = AbiCore(
{% if with_web_interface %}    web_interface_cls=AgentWebInterface,
    interface_name="{{ agent_display_name }} Web Interface",
{% endif %})

# Register tasks/tools with decorators (optional):
#
# @agent.task(name="step1")
# def step1(raw_input):
#     return {"cleaned": raw_input.strip()}
#
# @agent.task(
#     name="step2",
#     depends_on=["step1"],
#     input_map={"data": "$step1.result"},
# )
# def step2(data):
#     return {"stored": True}
#
# @agent.tool(name="search")
# def search(query):
#     """Search the database."""
#     return {"results": []}
#
# MCP remote tool (no local function needed):
# @agent.mcp_tool(
#     name="bigquery_search",
#     input_map={"query": "$input.user_query"},
# )

agent.run({{ agent_class_name }}())
